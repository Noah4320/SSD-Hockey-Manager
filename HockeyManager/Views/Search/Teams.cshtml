@model IEnumerable<HockeyManager.Models.HMTeam>


<table class="table">
    <thead>
        <tr>
            <th>
                Logo
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeamInfo.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeamInfo.Division)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeamInfo.Conference)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <img src=@Html.DisplayFor(modelItem => item.TeamInfo.logoUrl) alt="Logo" width="200" height="75">
            </td>
            <td>
                @Html.ActionLink(item.TeamInfo.Name, "Players", new {  id=item.Id  })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeamInfo.Division)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeamInfo.Conference)
            </td>
        </tr>
}
    </tbody>
</table>
